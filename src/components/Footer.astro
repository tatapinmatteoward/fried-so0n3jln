---
const currentYear = new Date().getFullYear();
const socialLinks = [
  { icon: 'fab fa-facebook', url: '#' },
  { icon: 'fab fa-instagram', url: '#' },
  { icon: 'fab fa-youtube', url: '#' },
  { icon: 'fab fa-tiktok', url: '#' }
];
---

<footer class="bg-[#8b0000] text-[#ffdb58] py-8 border-t-4 border-[#ffdb58]">
  <div class="container mx-auto px-4 text-center">
    <p class="text-lg font-bold">© {currentYear} Fried Faith. All rights reserved.</p>
    <div class="flex justify-center gap-6 mt-4">
      {socialLinks.map((link) => (
        <a href={link.url} class="hover:text-white transition-colors duration-300">
          <i class={link.icon + " text-2xl"}></i>
        </a>
      ))}
    </div>
    <p class="mt-4 text-sm">Celebrating bold cultural heritage through energetic storytelling</p>
  </div>
</footer>